apiVersion: v1
kind: Service
metadata:
  name: webmin
  namespace: default
  labels:
    app: webmin
spec:
  type: ExternalName
  externalName: {{ WEBMIN_SERVICE_HOSTNAME }}
  ports:
    - name: "https"
      port: 443
      targetPort: 10000
  selector:
    app: webmin