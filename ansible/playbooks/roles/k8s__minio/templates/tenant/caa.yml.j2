apiVersion: cloudflare.zelic.io/v1alpha1
kind: CloudflareAccessApplication
metadata:
  name: "{{ k8s__minio__external_hostname__console }}"
  namespace: minio-tenant
  annotations:
    # (optional) default: "false"
    #   ensures that the resource isn't removed from cloudflare if the CR is deleted
    cloudflare.zelic.io/prevent-destroy: "false"
spec:
  name: "{{ k8s__minio__external_hostname__console }}"
  domain: "{{ k8s__minio__external_hostname__console }}"
  autoRedirectToIdentity: true
  policies: 
    - name: Allow my rules
      decision: allow
      include:
        - accessGroups:
          - value: "accessgroup-root-domain"