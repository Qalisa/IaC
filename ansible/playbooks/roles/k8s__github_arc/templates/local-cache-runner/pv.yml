apiVersion: v1
kind: PersistentVolume
metadata:
  name: gha-cache
  labels:
    type: local
spec:
  storageClassName: "{{ k8s__github_arc_cache_storage_class_name }}"
  capacity:
    storage: "{{ k8s__github_arc_local_cache_size }}" 
  local:
    path: {{ pv_path__github_arc__cache }}
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - ReadWriteOnce #
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
          - key: kubernetes.io/hostname
            operator: In
            values:
              - {{ master_node_name }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gha-cache
  namespace: "{{ k8s__github_arc_systems_ns }}"
spec:
  storageClassName: {{ k8s__github_arc_cache_storage_class_name }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "{{ k8s__github_arc_local_cache_size }}"