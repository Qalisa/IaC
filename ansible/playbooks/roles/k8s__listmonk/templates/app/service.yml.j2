apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: listmonk
  labels:
    app: listmonk
spec:
  type: LoadBalancer
  selector:
    app: listmonk
  ports:
    - name: app
      port: 9000
      targetPort: app
      protocol: TCP