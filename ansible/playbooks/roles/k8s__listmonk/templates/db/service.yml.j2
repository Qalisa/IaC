apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: listmonk
  labels:
    app: listmonk-db
spec:
  type: ClusterIP
  selector:
    app: listmonk-db
  ports:
    # smtp
    - name: db
      port: 5432
      targetPort: db
      protocol: TCP