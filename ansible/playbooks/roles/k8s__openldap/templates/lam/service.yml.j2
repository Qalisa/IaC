apiVersion: v1
kind: Service
metadata:
  name: lam
  namespace: "{{ openldap_stack__namespace }}"
  labels:
    app: lam
spec:
  selector:
    app: lam
  ports:
    - name: app
      port: 80
      targetPort: app
      protocol: TCP