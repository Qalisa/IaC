apiVersion: v1
kind: Service
metadata:
  name: self-service-password
  namespace: openldap-stack
  labels:
    app: self-service-password
spec:
  selector:
    app: self-service-password
  ports:
    - name: app
      port: 80
      targetPort: app
      protocol: TCP