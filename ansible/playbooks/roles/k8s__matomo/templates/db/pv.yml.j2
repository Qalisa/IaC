apiVersion: v1
kind: PersistentVolume
metadata:
  name: matomo-db
  labels:
    type: local
    name: matomo-db
spec:
  storageClassName: "{{ local_storage_class_name }}"
  capacity:
    storage: 8Gi 
  local:
    path: {{ pv_path__matomo__db }}
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - ReadWriteOnce #
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
          - key: kubernetes.io/hostname
            operator: In
            values:
              - {{ master_node_name }}
