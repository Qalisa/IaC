apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: "{{ k8s_more__traefik__cf_tls_mode__full_strict__cert_resolver_name }}"
  namespace: "{{ k3s__default_namespace }}"
spec:
  defaultCertificate:
    secretName: cloudflare-origin-cert