apiVersion: v1
kind: Secret
metadata:
  name: "{{ k8s__cert_manager__dns01__solver_secret }}"
  namespace: "{{ cert_manager__namespace }}"
type: Opaque
stringData:
  api-token: "{{ iac__cloudflare__api_token }}"
