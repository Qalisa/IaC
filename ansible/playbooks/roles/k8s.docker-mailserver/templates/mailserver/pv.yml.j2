apiVersion: v1
kind: PersistentVolume
metadata:
  name: docker-mailserver
  labels:
    type: local
spec:
  storageClassName: "{{ local_storage_class_name }}"
  capacity:
    storage: 25Gi 
  local:
    path: {{ docker_mailserver_pv_path }}
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - ReadWriteOnce #
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
          - key: kubernetes.io/hostname
            operator: In
            values:
              - {{ master_node_name }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-mailserver
  namespace: docker-mailserver
spec:
  storageClassName: {{ local_storage_class_name }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi